<% provide(:title, @events.title) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= @events.creator.name %>
      </h1>
      
      <h1>
        <%= @events.title %>
      </h1>
        <p>
          <%= @events.description %>
        </p>
    </section>
  </aside>
</div>
